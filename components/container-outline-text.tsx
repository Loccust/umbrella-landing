import styled from "@emotion/styled";
import theme from "../lib/theme";

export const ContainerOutlineText = styled.h2`
  -webkit-text-stroke: 3px ${theme.colors.primary};
  -webkit-text-fill-color: transparent;
`;